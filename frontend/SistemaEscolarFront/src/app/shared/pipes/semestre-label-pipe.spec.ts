import { SemestreLabelPipe } from './semestre-label-pipe';

describe('SemestreLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SemestreLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
